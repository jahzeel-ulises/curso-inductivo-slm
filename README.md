# curso-inductivo-slm
Repositorio de código, clase curso inductivo para proyecto SLM.

Para utilizar las notebooks que aqui yacen srecomiendo usar un entorno virtual:

**En Windows:**
```bash
python -m venv .env
cd .env/Scripts
activate
pip install ipykernel
pip install pandas numpy matplotlib scikit-learn gensim nltk
```

**En macOS/Linux:**
```bash
python -m venv .env
source .env/bin/activate
pip install ipykernel
pip install pandas numpy matplotlib scikit-learn gensim nltk
```